node_modules
# Supabase local
supabase/.temp/
.next/
*.tsbuildinfo
.env
.env.local
.env*.local
.vercel

# Playwright walkthrough screenshots
tests/e2e/.artifacts/

# Project-local Playwright browser cache (PLAYWRIGHT_BROWSERS_PATH)
.playwright-cache/

# Supabase project migration artifacts (credentials, data dumps, generated SQL)
scripts/migrate/.env.migrate
scripts/migrate/dump/
scripts/migrate/generated/

# Freebuff thread workspace artifacts
.freebuff/

# Local data dumps (contain customer/order data — never commit)
backups/
